[project]
name = "memecoin-trading-bot"
version = "0.1.0"
description = "Risk-managed Solana meme coin trading bot with on-chain safety verification"
requires-python = ">=3.11"
dependencies = []

[project.optional-dependencies]
dev = ["pytest>=8.0"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = [".", "tests"]
